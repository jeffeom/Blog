<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="<%= root_path %>"><i class="glyphicon glyphicon-home"></i> Home</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "<i class='glyphicon glyphicon-user'></i> About".html_safe, about_path %></li>
        <li><%= link_to "<i class='glyphicon glyphicon-plus'></i>New Post".html_safe, new_post_path %></li>
        <li><%= link_to "<i class='glyphicon glyphicon-plus'></i>Posts".html_safe, posts_path %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"> User <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="#">Profile</a></li>
            <li class="divider"></li>
            <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
      </ul>
      <ul class="navbar-form navbar-right">
          <% if !user_signed_in? %>
            <%= link_to "LOG IN", new_user_session_path%>
            <%= link_to "CREATE FREE ACCOUNT", new_user_registration_path %>
          <% else %>
            <%= link_to "LOG OUT", destroy_user_session_path, method: :delete %>
          <% end %>
      </ul>
    </div>
  </div>
</nav>
